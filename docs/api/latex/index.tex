\chapter{Outdated Readme}
\hypertarget{index}{}\label{index}\index{Outdated Readme@{Outdated Readme}}
\label{index_md__r_e_a_d_m_e}%
\Hypertarget{index_md__r_e_a_d_m_e}%


This Readme file belongs to the first alpha versions of Kryptor, The public API of this library heavily changed and this file will be updated on first stable release.\hypertarget{index_autotoc_md6}{}\doxysection{\texorpdfstring{Kryptor}{Kryptor}}\label{index_autotoc_md6}
\href{https://github.com/SAPTeamDEV/Kryptor/actions/workflows/main.yml}{\texttt{ }} \href{https://github.com/SAPTeamDEV/Kryptor/actions/workflows/codeql.yml}{\texttt{ }} \href{https://codecov.io/gh/SAPTeamDEV/Kryptor}{\texttt{ }} \href{https://www.nuget.org/packages/SAPTeam.Kryptor}{\texttt{ }} \href{https://www.nuget.org/packages/SAPTeam.Kryptor}{\texttt{ }}

Kryptor is a {\bfseries{key-\/based encryption engine}} that currently uses the {\bfseries{KES}} encryption algorithm to encrypt and decrypt data.\hypertarget{index_autotoc_md7}{}\doxysubsection{\texorpdfstring{What is the KES?}{What is the KES?}}\label{index_autotoc_md7}
KES is a complex encryption algorithm that uses a keystore to encrypt and decrypt data.

A keystore can contain up to 128 keys, and each key is used to encrypt and decrypt parts of data. Every 32 bytes of data is encrypted using a different key, and the keys are used in a round-\/robin fashion. in other words, the first 32 bytes of data are encrypted using the first key, the next 32 bytes are encrypted using the second key, and so on. When the end of the keystore is reached, the encryption process starts over from the beginning of the keystore.

In this way, in a 128-\/key keystore, the first 128\texorpdfstring{$\ast$}{*}32 bytes of data are encrypted using the first 128 keys, and then the process starts over from the beginning of the keystore.

so in a 1 MB file, each key is used to encrypt and decrypt 8 KB of non-\/consecutive data. that makes it very difficult for an attacker to decrypt the data without the keystore. because the attacker would have to guess all 128 keys in the keystore to decrypt the data.

Each key has a length of 256 bits, so there are 2\texorpdfstring{$^\wedge$}{\string^}256 possible options to guess, just for 1 key in the keystore, which makes it impossible for an attacker to guess all the keys.\hypertarget{index_autotoc_md8}{}\doxysubsection{\texorpdfstring{How to use the KES?}{How to use the KES?}}\label{index_autotoc_md8}
To use the KES, you need to create a keystore using the KES Keystore Generator, and then use the keystore to encrypt and decrypt data using the KESProvider.


\begin{DoxyEnumerate}
\item Create a keystore using the KES Keystore Generator ~\newline

\end{DoxyEnumerate}


\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{preprocessor}{\ \#}}
\DoxyCodeLine{KESKeyStore\ ks\ =\ KESKeyStore.Generate();}

\end{DoxyCode}



\begin{DoxyEnumerate}
\item Save the keystore to a file to use it later
\end{DoxyEnumerate}


\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{preprocessor}{\ \#}}
\DoxyCodeLine{File.WriteAllLines(\textcolor{stringliteral}{"{}keystore.kks"{}},\ \textcolor{keyword}{new}\ \textcolor{keywordtype}{string}[]\ \{ks.ToString()\});}

\end{DoxyCode}



\begin{DoxyEnumerate}
\item Encrypt data using the KESProvider 
\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{preprocessor}{\ \#}}
\DoxyCodeLine{KESProvider\ kp\ =\ \textcolor{keyword}{new}\ KESProvider(ks);}
\DoxyCodeLine{kp.EncryptFile(\textcolor{stringliteral}{"{}InputFile"{}},\ \textcolor{stringliteral}{"{}OutputFile"{}});}

\end{DoxyCode}

\end{DoxyEnumerate}

and to decrypt the data, use the same keystore to decrypt the data


\begin{DoxyEnumerate}
\item Load the keystore from the file 
\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{preprocessor}{\ \#}}
\DoxyCodeLine{KESKeyStore\ ks\ =\ KESKeyStore.FromString(File.ReadAllText(\textcolor{stringliteral}{"{}keystore.kks"{}}));}

\end{DoxyCode}

\item Decrypt the data using the KESProvider 
\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{preprocessor}{\ \#}}
\DoxyCodeLine{KESProvider\ kp\ =\ \textcolor{keyword}{new}\ KESProvider(ks);}
\DoxyCodeLine{kp.DecryptFile(\textcolor{stringliteral}{"{}InputFile"{}},\ \textcolor{stringliteral}{"{}OutputFile"{}});}

\end{DoxyCode}

\end{DoxyEnumerate}

Note\+: You can also use the KESProvider to encrypt and decrypt data in memory with the Encrypt\+Block and Decrypt\+Block methods. 